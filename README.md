# Bill_Receipt
This Python program simulates grocery billing. It uses a dictionary for inventory and a loop to add items and quantities to a cart. It validates inputs, calculates the subtotal, applies a 5% GST, and then prints an itemized receipt showing the grand total.
